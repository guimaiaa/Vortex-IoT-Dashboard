#pragma once

// Copy this file to secrets.h and fill in your real credentials.
// secrets.h is gitignored so your WiFi password never gets committed.
#define WIFI_SSID "YOUR_WIFI_SSID"
#define WIFI_PASSWORD "YOUR_WIFI_PASSWORD"
